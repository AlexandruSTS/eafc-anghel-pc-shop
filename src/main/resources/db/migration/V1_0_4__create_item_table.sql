CREATE TABLE ITEM (
                      ID BIGINT PRIMARY KEY,
                      NAME VARCHAR(255) NOT NULL,
                      DESCRIPTION VARCHAR(255),
                      PRICE DECIMAL(10, 2) NOT NULL
);